#include "List.h"
#include <iostream>
#include "List-ListIterator.hpp"

using namespace std;

int main() {
	/*
	List<unsigned> list;
  	for (unsigned i = 0; i < 10; i++) { list.insertBack(i); }
  	list.reverse();
  	for (ListIterator<unsigned> yes = list.begin(); yes != list.end(); ++yes) {
  		cout << *yes << endl;
  	}
  	*/
  	return 0;
}
